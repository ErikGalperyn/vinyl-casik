# 🎵 Spotify API Setup

## Шаг 1: Создание Spotify приложения

1. Перейдите на https://developer.spotify.com/dashboard
2. Войдите в свой Spotify аккаунт (бесплатный)
3. Нажмите **"Create app"**
4. Заполните форму:
   - **App name**: Medioteka
   - **App description**: Vinyl collection manager
   - **Redirect URI**: `http://localhost:4001/callback` (можно не использовать)
   - **API**: отметьте "Web API"
5. Примите Terms of Service
6. Нажмите **"Save"**

## Шаг 2: Получение credentials

1. В созданном приложении нажмите **"Settings"**
2. Скопируйте:
   - **Client ID** (виден сразу)
   - **Client Secret** (нажмите "View client secret")

## Шаг 3: Установка в проект

### Вариант 1: Через переменные окружения (рекомендуется)

Создайте файл `.env` в папке `backend/`:

```bash
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here
```

Установите dotenv:
```bash
cd backend
npm install dotenv
```

Добавьте в начало `server.js`:
```javascript
require('dotenv').config();
```

### Вариант 2: Напрямую в код (для тестов)

Откройте `backend/server.js` и замените строки:

```javascript
const SPOTIFY_CLIENT_ID = process.env.SPOTIFY_CLIENT_ID || 'ваш_client_id';
const SPOTIFY_CLIENT_SECRET = process.env.SPOTIFY_CLIENT_SECRET || 'ваш_client_secret';
```

На:

```javascript
const SPOTIFY_CLIENT_ID = process.env.SPOTIFY_CLIENT_ID || 'ваш_реальный_client_id';
const SPOTIFY_CLIENT_SECRET = process.env.SPOTIFY_CLIENT_SECRET || 'ваш_реальный_client_secret';
```

## Шаг 4: Тестирование

1. Перезапустите backend сервер:
```bash
cd backend
npm start
```

2. Откройте frontend (должен быть запущен):
```bash
cd frontend
npm run dev
```

3. В приложении:
   - Нажмите "Add Vinyl"
   - Введите в поле поиска: "Daft Punk Random Access"
   - Нажмите "Search"
   - Выберите трек из результатов
   - Форма автоматически заполнится!

## Что работает:

✅ Поиск треков по названию/исполнителю
✅ Автозаполнение: название, исполнитель, год, обложка
✅ 30-секундное превью музыки (если доступно от Spotify)
✅ Без ограничений на количество запросов (для разумного использования)

## Ограничения бесплатного API:

- **180 запросов в минуту** - более чем достаточно
- Превью треков **только 30 секунд** (это официальное ограничение Spotify)
- Для полных треков нужен Spotify Premium + OAuth (сложнее)

## Безопасность:

⚠️ **НЕ коммитьте** `.env` файл в git!

Добавьте в `.gitignore`:
```
.env
```
